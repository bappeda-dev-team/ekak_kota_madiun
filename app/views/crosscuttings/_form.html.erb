<%= form_with(model: crosscutting) do |form| %>
  <% if crosscutting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crosscutting.errors.count, "error") %> prohibited this crosscutting from being saved:</h2>

      <ul>
        <% crosscutting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :internal_external %>
    <%= form.text_field :internal_external %>
  </div>

  <div class="field">
    <%= form.label :opd_list %>
    <%= form.text_field :opd_list %>
  </div>

  <div class="field">
    <%= form.label :keterangan %>
    <%= form.text_field :keterangan %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
