bootstrap.Modal.getInstance($('#form-inovasi')).hide()
$('#form-inovasi').on('hidden.bs.modal', function () {
  bootstrap.Modal.getInstance($('#form-inovasi')).dispose()
  Swal.fire({
    title: 'Usulan berhasil diupdate',
    icon: 'success',
    confirmButtonText: 'Yes',
  }).then(() => {
    Turbolinks.visit(window.location, { action: "replace" })
  })
})
