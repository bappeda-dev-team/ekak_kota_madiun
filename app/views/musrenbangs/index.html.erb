<div class="row">
  <div class="col-12">
    <nav aria-label="breadcrumb" class="d-none d-md-inline-block mb-3">
      <ol class="breadcrumb breadcrumb-dark breadcrumb-transparent mb-0">
        <li class="breadcrumb-item">
          <%= link_to root_path do %>
            <i class="fas fa-home"></i>
          <% end %>
        </li>
        <li class="breadcrumb-item active" aria-current="page"><%= link_to "Musrenbang", musrenbangs_path %></li>
      </ol>
    </nav>
    <div class="card border-0 shadow">
      <div class="card-header">
        <div class="row align-items-center">
          <div class="col">
            <h2 class="fs-5 fw-bold mb-0">Usulan Musrenbang </h2>
          </div>
          <div class="col text-end">
            <%= link_to "Tambah Usulan", new_musrenbang_path, remote: true, data: {'bs-toggle': 'modal', 'bs-target': '#form-usulan'}, class: "btn btn-sm btn-info" %>
          </div>
        </div>
      </div>
      <div class="card-body">
        <div class="table-responsive">
          <table class="table table-flush datatable" data-datatable>
            <thead class="thead-light">
              <tr>
                <th class="border-bottom" scope="col">No.</th>
                <th class="border-bottom" scope="col">Usulan</th>
                <th class="border-bottom" scope="col">Alamat</th>
                <th class="border-bottom text-center" scope="col">Active ?</th>
                <th class="border-bottom text-center" scope="col" colspan='2'>Action</th>
              </tr>
            </thead>
            <tbody>
              <% @musrenbangs.each.with_index(1) do |musrenbang, i| %>
                <tr>
                  <td class="text-gray-900 fw-bolder" scope="row"><%= i %></td>
                  <td class="fw-bolder text-gray-500"><%= truncate(musrenbang.usulan, length: 30, separator: '...') %></td>
                  <td class="fw-bolder text-gray-500"><%= truncate(musrenbang.alamat, length: 25, separator: '...') %></td>
                  <td class="fw-bolder text-center" id="musren-<%= musrenbang.id %>"><%= status_icon(musrenbang.is_active) %></td>
                  <td class="action-<%= musrenbang.id %> text-center">
                    <% unless musrenbang.is_active %>
                      <%= link_to "/aktifkan_usulan/#{musrenbang.id}", method: :patch, remote: true do %>
                        <i class="fas fa-check-square text-success" data-bs-toggle="tooltip" data-bs-placement="bottom" title="Aktifkan"></i>
                      <% end %>
                    <% else %>
                      <%= link_to "/non_aktifkan_usulan/#{musrenbang.id}", method: :patch, remote: true do %>
                        <i class="fas fa-times-circle text-danger" data-bs-toggle="tooltip" data-bs-placement="bottom" title="Non Aktifkan"></i>
                      <% end %>
                    <% end %>
                  </td>
                  <td class="fw-bolder text-gray-500 text-center">
                    <%= link_to edit_musrenbang_path(musrenbang), remote: true, data: {'bs-toggle': 'modal', 'bs-target': '#form-usulan'} do %>
                      <i class="fas fa-edit" data-bs-toggle="tooltip" data-bs-placement="bottom" title="Edit usulan"></i>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
<%= render 'modal' %>
