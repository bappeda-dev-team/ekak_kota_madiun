<% presenter = PohonKotaPresenter.new(pohon) %>
<% collections = @tactical_tematiks.select { |str| str.pohon_ref_id == pohon.id } %>
<li>
  <div class="tf-nc strategic-pohon" style="width: 450px;">
    <%= render partial: 'cetaks/kota/cascading/item_cascading_tematik', locals: { presenter: presenter } %>
  </div>
  <% if collections.any? %>
    <ul class='childs'>
      <%= render partial: 'cetaks/kota/cascading/cascading_tactical.html.erb',
                 collection: collections,
                 as: :pohon %>
    </ul>
  <% end %>
</li>
