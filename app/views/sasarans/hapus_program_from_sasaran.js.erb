Swal.fire({
  title: "<%= @status.capitalize %>",
  text: "<%= @text %>",
  icon: "<%= @status %>",
  timer: 1000,
  showConfirmButton: false,
}).then(() => {
  Turbolinks.visit(window.location, { action: "replace" })
})