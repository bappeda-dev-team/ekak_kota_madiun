<div class="card border-2 shadow mb-3" id="musrenbang-card">
  <div class="card-header">
    <div class="row align-items-center">
      <div class="row align-items-center">
        <div class="col">
          <h5>Musrenbang Terhubung </h5>
        </div>
        <%= form_with(url: '/update_sasaran_asn', local: false) do |form| %>
          <div class="field">
            <%= form.hidden_field :sasaran_id, value: sasaran.id   %>
            <%= form.label :usulan %>
            <%= form.select :id, [], {include_blank: true}, {id: "select2-musrenbang", "data-placeholder": "Pilih Usulan"} %>
          </div>
          <div class="actions">
            <%= form.submit %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="card-body">
    <table class="table" id="musrenbang-table">
      <thead>
        <tr>
          <th>No</th>
          <th>Usulan</th>
        </tr>
      </thead>
      <tbody>
        <% sasaran.musrenbangs.each.with_index(1) do |sas, i| %>
          <tr>
            <td><%= i %></td>
            <td><%= sas.usulan %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
