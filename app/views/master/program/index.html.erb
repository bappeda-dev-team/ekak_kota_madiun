<div class="row">
  <div class="col-12">
    <nav aria-label="breadcrumb" class="d-none d-md-inline-block mb-3">
      <ol class="breadcrumb breadcrumb-dark breadcrumb-transparent mb-0">
        <li class="breadcrumb-item">
          <%= link_to root_path do %>
            <i class="fas fa-home"></i>
          <% end %>
        </li>
        <li class="breadcrumb-item active" aria-current="page">Master Program</li>
      </ol>
    </nav>
    <div class="card border-0 shadow">
      <div class="card-header">
        <div class="row align-items-center">
          <div class="col">
            <h2 class="fs-5 fw-bold mb-0">Master Program</h2>
          </div>
          <div class="col text-end">
            <%= link_to '/sync_master_program/2023',
                                                  method: :post,
                                                  data: { disable_with: 'Memproses....'},
                                                  class:"btn btn-sm btn-primary" do %>
              <i class="fas fa-sync me-2"></i>
              Update Master Program tahun-x
            <% end %>
          </div>
        </div>
      </div>
      <div class="table-responsive">
        <table class="table align-items-center">
          <thead class="thead-light">
            <tr>
              <th class="border-bottom" scope="col">No.</th>
              <th class="border-bottom" scope="col">No Program</th>
              <th class="border-bottom" scope="col">Kode Program</th>
              <th class="border-bottom" scope="col">Nama Program</th>
              <th class="border-bottom" scope="col">Tahun</th>
              <th class="border-bottom" scope="col">id_program</th>
            </tr>
          </thead>
          <tbody>
            <% @programs.each.with_index(1) do |prog, i| %>
              <tr>
                <td class="text-gray-900 fw-bolder" scope="row"><%= i %></td>
                <td class="fw-bolder text-gray-500" style="white-space: initial !important;"><%= prog.no_program %></td>
                <td class="fw-bolder text-gray-500" style="white-space: initial !important;"><%= prog.kode_program %></td>
                <td class="text-gray-900" scope="row" style="white-space: initial !important;"><%= prog.nama_program %></td>
                <td class="fw-bolder text-gray-500" style="white-space: initial !important;"><%= prog.tahun %></td>
                <td class="fw-bolder text-gray-500" style="white-space: initial !important;"><%= prog.id_program_sipd %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
