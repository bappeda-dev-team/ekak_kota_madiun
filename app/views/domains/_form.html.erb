<%= form_with(model: domain, local: false,
             data: { controller: 'form-ajax',
                    action: 'ajax:complete->form-ajax#afterSubmitRefresh'}) do |form| %>
  <% if domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(domain.errors.count, "error") %> prohibited this domain from being saved:</h2>

      <ul>
        <% domain.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :domain, class: 'form-label'%>
    <%= form.text_field :domain, class: 'form-control'%>
  </div>

  <div class="field">
    <%= form.label :kode_domain, class: 'form-label' %>
    <%= form.text_field :kode_domain, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :keterangan, class: 'form-label' %>
    <%= form.text_field :keterangan, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :tahun, class: 'form-label' %>
    <%= form.text_field :tahun, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
