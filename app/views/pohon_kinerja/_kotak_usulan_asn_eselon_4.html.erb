<div class="card border-2 shadow mb-3" id="musrenbang-card">
  <div class="card-header">
    <div class="row align-items-center">
      <div class="col">
        <h5>Strategi ASN</h5>
      </div>
      <div class="col-auto">
        <%= render 'shared/button_cog',
          link: new_strategi_path(nip: current_user.nik, role: "eselon_4"),
          name: "Buat Operational Objective" %>
      </div>
    </div>
  </div>
  <div class="card-body">
    <div class="table-responsive">
      <table class="table counter" id="isu-strategis-kota-table">
        <thead>
          <tr>
            <th>No</th>
            <th>Operational Objective</th>
            <th class="text-center">Tahun</th>
            <th class="text-center">NIP</th>
            <th class="text-center">Role</th>
            <th class="text-center">Aksi</th>
          </tr>
        </thead>
        <tbody>
          <% @strategis.each do |strategi| %>
            <% pohon = strategi.strategi_atasan %>
            <tr>
              <td class="text-wrap align-middle"></td>
              <td class="text-wrap align-middle"><%= strategi.strategi_asn %></td>
              <td class="text-wrap align-middle"><%= strategi.tahun %></td>
              <td class="text-wrap align-middle"><%= strategi.nip_asn %></td>
              <td class="text-wrap align-middle"><%= nama_eselon(strategi.role) %></td>
              <% unless pohon.nil? %>
                <td class="text-center align-middle">
                  <%= render 'tombol_aksi_pohon',
                  strategi: strategi,
                  pohon: pohon,
                  judul_tombol: 'Operational Objective',
                  role: strategi.role,
                  role_bawahan: local_assigns[:role_bawahan]
                %>
                </td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
