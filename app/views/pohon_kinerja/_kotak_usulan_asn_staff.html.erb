<div class="card border-2 shadow mb-3" id="musrenbang-card">
  <div class="card-header">
    <div class="row align-items-center">
      <div class="col">
        <h5>Strategi ASN</h5>
      </div>
      <div class="col-3">
        <%= render 'shared/button_cog',
          link: new_strategi_path(nip: current_user.nik, role: "staff"),
          name: "Buat Operational Objective 2" %>
      </div>
    </div>
  </div>
  <div class="card-body">
    <div class="table-responsive">
      <table class="table" id="isu-strategis-kota-table">
        <thead>
          <tr>
            <th>Operational Objective</th>
            <th>Operational Objective 2</th>
            <th>Tahun</th>
            <th>NIP</th>
            <th>Role</th>
            <th class="text-center align-middle">Aksi</th>
          </tr>
        </thead>
        <tbody>
            <% @strategis.each do |strategi| %>
              <% pohon = strategi.strategi_atasan %>
            <tr>
              <td class="text-wrap align-middle"><%= pohon.strategi %></td>
              <td class="text-wrap align-middle"><%= strategi.strategi %></td>
              <td class="text-wrap align-middle"><%= strategi.tahun %></td>
              <td class="text-wrap align-middle"><%= strategi.nip_asn %></td>
              <td class="text-wrap align-middle"><%= strategi.role %></td>
              <td class="text-center align-middle">
                <%= link_to edit_strategi_path(strategi, nip: strategi.nip_asn, usulan_isu: pohon.strategi, role: "staff"), class: "btn btn-outline-info mx-3" do %>
                  <i class="fas fa-edit me-2"></i>
                  Edit Operational Objective 2
                <% end %>
                <%= link_to strategi, method: :delete, data: { confirm_swal: "Hapus Strategi?"}, class: "btn btn-outline-danger mx-3" do %>
                  <i class="fas fa-edit me-2"></i>
                  Hapus Operational Objective 2
                <% end %>
              </td>
            </tr>
            <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
