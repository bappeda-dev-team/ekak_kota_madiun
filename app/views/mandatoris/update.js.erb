bootstrap.Modal.getInstance($('#form-mandatori')).hide()
$('#form-mandatori').on('hidden.bs.modal', function () {
  bootstrap.Modal.getInstance($('#form-mandatori')).dispose()
  Swal.fire({
    title: 'Usulan berhasil diupdate',
    icon: 'success',
    confirmButtonText: 'Yes',
  }).then(() => {
    Turbolinks.visit(window.location, { action: "replace" })
  })
})
