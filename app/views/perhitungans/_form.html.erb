<%= form_with(model: perhitungan) do |form| %>
  <% if perhitungan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(perhitungan.errors.count, "error") %> prohibited this perhitungan from being saved:</h2>

      <ul>
        <% perhitungan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :koefisien %>
    <%= form.text_field :koefisien %>
  </div>

  <div class="field">
    <%= form.label :volume %>
    <%= form.number_field :volume %>
  </div>

  <div class="field">
    <%= form.label :satuan %>
    <%= form.text_field :satuan %>
  </div>

  <div class="field">
    <%= form.label :harga %>
    <%= form.number_field :harga %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
