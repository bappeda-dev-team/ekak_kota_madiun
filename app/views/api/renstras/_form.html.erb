<%= form_with(model: api_renstra) do |form| %>
  <% if api_renstra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_renstra.errors.count, "error") %> prohibited this api_renstra from being saved:</h2>

      <ul>
        <% api_renstra.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
