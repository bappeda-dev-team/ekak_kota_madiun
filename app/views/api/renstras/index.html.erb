<p id="notice"><%= notice %></p>

<h1>Api Renstras</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @api_renstras.each do |api_renstra| %>
      <tr>
        <td><%= link_to 'Show', api_renstra %></td>
        <td><%= link_to 'Edit', edit_api_renstra_path(api_renstra) %></td>
        <td><%= link_to 'Destroy', api_renstra, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Api Renstra', new_api_renstra_path %>
