<%= form_with(model: [user, kak]) do |form| %>
  <% if kak.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kak.errors.count, "error") %> prohibited this kak from being saved:</h2>
      <ul>
        <% kak.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dasar_hukum %>
    <%= form.text_area :dasar_hukum %>
  </div>
  <div class="field">
    <%= form.label :dasar_hukum %>
    <%= form.text_area :dasar_hukum %>
  </div>

  <div class="field">
    <%= form.label :tujuan %>
    <%= form.text_area :tujuan %>
  </div>
  <div class="field">
    <%= form.label :tujuan %>
    <%= form.text_area :tujuan %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
