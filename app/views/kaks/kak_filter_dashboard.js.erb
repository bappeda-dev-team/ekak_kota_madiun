Swal.fire({
  title: "<%= @message %>",
  icon: "<%= @icon %>",
  confirmButtonText: 'Ok',
})
$('#user_filter').html("<%= j render 'kaks/' + @filter_file, program_kegiatans: @program_kegiatans, tahun: @tahun %>");